import { LoginPanel } from "~/components/LoginPanel";

export default function Login() {
  return (
    <div className="m-auto flex h-screen w-screen flex-col items-center justify-center bg-primary">
      <LoginPanel />
    </div>
  );
}
